[pytest]
testpaths = control/tests python-sdk/tests tests/integration
markers =
    extended: opt-in long-running test (large transfers, sustained load)
    root: requires root privileges (network namespaces)
filterwarnings =
    ignore::DeprecationWarning:starlette.*
